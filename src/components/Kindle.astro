---
import { GET } from "../pages/kindle";
import KindleBook from "./KindleBook.astro";

const response = await GET().then((response: any) => response?.json());
---

{
  response?.length ? (
    <div class="flex flex-col gap-4">
      <div class="text-lg font-medium flex gap-3 text-slate-800 dark:text-slate-200">
        📚 My Readings
      </div>
      <div class="grid grid-cols-[repeat(auto-fill,minmax(120px,1fr))] gap-5">
        {response?.books?.map((book: any) => (
          <KindleBook book={book} />
        ))}
      </div>
    </div>
  ) : null
}
